'use client'; import Link from 'next/link'; import {ArrowLeft,ArrowUpRight,CheckCircle2,ImagePlus,Leaf,UploadCloud} from 'lucide-react'; import {useRef,useState} from 'react';
export default function Doctor(){const ref=useRef<HTMLInputElement>(null);const[file,setFile]=useState<File|null>(null);const[loading,setLoading]=useState(false);const[done,setDone]=useState(false);function analyze(){if(!file)return;setLoading(true);setTimeout(()=>{setLoading(false);setDone(true)},1200)}return <main className="min-h-screen bg-[#f7f5ee] text-[#16251b]"><div className="mx-auto max-w-[1050px] px-5 py-8 sm:px-8"><Link href="/dashboard" className="inline-flex items-center gap-2 text-sm text-black/45"><ArrowLeft size={16}/> Dashboard</Link><div className="mt-9 text-center"><div className="mx-auto grid h-14 w-14 place-items-center rounded-2xl bg-[#0d5b36] text-white"><Leaf size={25}/></div><div className="mt-5 text-xs font-bold uppercase tracking-[.18em] text-[#0d5b36]">Crop Doctor</div><h1 className="mt-2 text-4xl font-bold tracking-[-.06em]">What&apos;s happening to your crop?</h1><p className="mx-auto mt-3 max-w-xl text-sm leading-6 text-black/45">Upload a clear photo of an affected leaf, stem or fruit.</p></div><div className="mx-auto mt-9 grid max-w-3xl gap-5 md:grid-cols-2"><div onClick={()=>ref.current?.click()} className="cursor-pointer rounded-[28px] border-2 border-dashed border-[#0d5b36]/15 bg-white p-8 text-center hover:border-[#0d5b36]/35">{file?<><div className="mx-auto grid h-16 w-16 place-items-center rounded-2xl bg-[#e8f2ea] text-[#0d5b36]"><CheckCircle2 size={28}/></div><h2 className="mt-5 font-bold">{file.name}</h2><p className="mt-2 text-xs text-black/40">Click to replace</p></>:<><div className="mx-auto grid h-16 w-16 place-items-center rounded-2xl bg-[#e8f2ea] text-[#0d5b36]"><ImagePlus size={27}/></div><h2 className="mt-5 font-bold">Upload crop photo</h2><p className="mt-2 text-xs text-black/40">JPG or PNG · clear daylight photos work best</p><div className="mx-auto mt-5 flex w-fit items-center gap-2 rounded-full bg-[#f1f3ed] px-4 py-2 text-xs font-semibold"><UploadCloud size={15}/> Choose image</div></>}<input ref={ref} type="file" accept="image/*" hidden onChange={e=>setFile(e.target.files?.[0]||null)}/></div><div className="rounded-[28px] bg-[#113f29] p-8 text-white"><div className="text-xs font-bold uppercase tracking-[.16em] text-white/40">How it works</div><div className="mt-6 space-y-5">{['Upload a crop image','ML model checks visual patterns','AgroSense estimates likely issue','Receive a practical next step'].map((x,i)=><div key={x} className="flex gap-3"><span className="grid h-7 w-7 shrink-0 place-items-center rounded-full bg-white/10 text-[10px] font-bold">{i+1}</span><span className="pt-1 text-sm text-white/70">{x}</span></div>)}</div></div></div>{file&&<button onClick={analyze} disabled={loading} className="mx-auto mt-5 flex w-full max-w-3xl items-center justify-center gap-2 rounded-2xl bg-[#0d5b36] py-4 text-sm font-semibold text-white disabled:opacity-60">{loading?'Analyzing image...':'Analyze with AgroSense'} {!loading&&<ArrowUpRight size={16}/>}</button>}{done&&<div className="mx-auto mt-5 max-w-3xl rounded-[28px] border border-[#cde1cf] bg-white p-7"><div className="flex items-center gap-2 text-xs font-bold text-[#0d6b3f]"><CheckCircle2 size={16}/> Analysis complete</div><h2 className="mt-4 text-2xl font-bold">Possible early blight pattern</h2><span className="mt-3 inline-block rounded-full bg-[#e7f2e9] px-3 py-1 text-xs font-semibold text-[#0d6b3f]">Confidence 87%</span><p className="mt-5 text-sm leading-6 text-black/50">The prototype detected visual features associated with early blight. Inspect nearby plants and verify the diagnosis with local agricultural expertise before treatment.</p><div className="mt-6 rounded-2xl bg-[#edf3e9] p-5"><div className="text-sm font-bold">Recommended next step</div><p className="mt-2 text-xs leading-5 text-black/45">Check affected plants, improve airflow where appropriate, and avoid unnecessary overhead watering.</p></div></div>}</div></main>}
